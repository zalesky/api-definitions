description: SMSVoucher Gateway config.
allOf:
  - $ref: ../GatewayAccountCommon.yaml
  - type: object
    required:
      - credentials
    properties:
      credentials:
        type: object
        properties:
          AppId:
            type: string
            description: SMSVoucher AppId.
        required:
          - AppId
